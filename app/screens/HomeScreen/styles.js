import {StyleSheet} from 'react-native';
import {veryDarkGrey} from '../../theme/colors';

export const styles = StyleSheet.create({
  container: {
    flex: 1,
    backgroundColor: veryDarkGrey,
  },
  FABIconStyle: {
    fontSize: 22,
    fontWeight: 'bold',
  },
});
